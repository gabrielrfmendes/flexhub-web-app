import styled from 'styled-components';

const View = styled.div({
    backgroundColor: 'purple',
    height: '100%',
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
});

export default View;